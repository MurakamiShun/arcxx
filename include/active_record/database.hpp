#pragma once
#include <string>
#include <string_view>
#include <chrono>
#include "utils.hpp"

namespace active_record{
    class database{
    private:
    public:
        active_record::string schema();
    };
}